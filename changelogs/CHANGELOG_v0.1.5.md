# Changelog

## [v0.1.5]

### Added
* Added syntax highlighting for `optional chaining` and `nullish coalescing` operators usable in conjunction with objects and access chains (`?.` and `??`).

### Fixed
* Fixed a regression where `bitwise operators` could incorrectly be highlighted as access chains.
* Fixed a regression where `if`-conditions could be highlighted as function calls under certain circumstances.